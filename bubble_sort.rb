def bubble_sort(array)
    #keep track of 1st operand, 2nd operand, 
    #iterate through array
    #go through two indexes at a time account for length
    #check 1st and second, then second and third
    #shift up if left number is bigger
end
